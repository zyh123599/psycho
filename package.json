{
  "name": "psycho-capacitor",
  "version": "0.1.0",
  "private": true,
  "description": "心潮心理叙事原型的 Capacitor Android 应用",
  "scripts": {
    "cap:sync": "cap sync android",
    "android:open": "cap open android",
    "android:run": "cap run android",
    "android:build": "npm run cap:sync && cd android && JAVA_HOME=${JAVA_HOME:-/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home} GRADLE_USER_HOME=${GRADLE_USER_HOME:-../.gradle-home} ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
